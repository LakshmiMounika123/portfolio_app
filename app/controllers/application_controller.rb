class ApplicationController < ActionController::Base

    # require 'filewatcher'
   
    #     Filewatcher.new(['lib/', 'ftp']).watch do |filename, event|
    #         puts "#{filename} #{event}"
    #     end
    # listener = Listen.to('project/ftpprogram.rb') do |item|
    #     puts "#{item}"
    # end
    # listener.start
end
